table! {
    users (id) {
        id -> Integer,
        username -> Varchar,
        password -> Varchar,
        first_name -> Varchar,
        sharesecret -> Varchar,
        authenticator_source -> Varchar,
    }
}
